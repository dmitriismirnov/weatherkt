<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/toolbar_size"
    android:background="@color/toolbar_light_background_color">


    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/toolbar_icon_menu_btn"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:paddingEnd="@dimen/toolbar_padding"
        android:paddingStart="@dimen/toolbar_padding"
        android:src="@drawable/v_ic_menu" />


    <TextView
        android:id="@+id/toolbar_nav_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/app_name"
        android:textColor="@color/toolbar_light_main_color"
        android:textSize="@dimen/text_size_default" />

    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/toolbar_icon_search_btn"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:paddingEnd="@dimen/toolbar_padding"
        android:paddingStart="@dimen/toolbar_padding"
        android:src="@drawable/v_ic_search" />

</RelativeLayout>